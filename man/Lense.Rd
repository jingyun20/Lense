% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Lense.R
\name{Lense}
\alias{Lense}
\title{Lense: Optimizing Data Preprocessing in Single-Cell Omics Using LLMs}
\usage{
Lense(
  seurat_obj,
  normalization_methods = c("SCT", "None", "LogNormalize", "LogCount", "RC", "CLR"),
  nfeatures_vals = c("HVG", "all"),
  pc_vals = c(5, 20),
  resolution_vals = c(0.2, 0.5, 1),
  api_key = Sys.getenv("OPENAI_API_KEY"),
  output_dir = NULL,
  verbose = FALSE
)
}
\arguments{
\item{seurat_obj}{A Seurat object after cell filtering and quality control.}

\item{normalization_methods}{Character vector of normalization methods to evaluate. Default includes
\code{c("SCT", "None", "LogNormalize", "CLR", "RC", "LogCount")}.}

\item{nfeatures_vals}{Character vector specifying feature selection strategies to evaluate.
Accepts \code{"HVG"} (select highly variable genes) and \code{"all"} (use all genes).}

\item{pc_vals}{A numeric vector specifying the number of principal components to evaluate (default: \code{c(5, 20)}).}

\item{resolution_vals}{Numeric vector of clustering resolution values to try (default: \code{c(0.2, 0.5, 1)}).}

\item{api_key}{Your OpenAI API key. Defaults to the environment variable \code{OPENAI_API_KEY}.}

\item{output_dir}{Directory to save UMAP plots. If \code{NULL}, uses a system temporary directory.}

\item{verbose}{Logical. If \code{TRUE}, print intermediate progress messages. Default is \code{FALSE}.}
}
\value{
A Seurat object corresponding to the best-performing preprocessing pipeline selected by GPT-4o.
}
\description{
Applies multiple preprocessing pipelines to a single-cell Seurat object,
generates UMAP plots, and selects the optimal pipeline using GPT-4o-based visual comparison.
All UMAP plots are saved automatically. The function returns the best Seurat object
corresponding to the optimal preprocessing pipeline.
}
\examples{
\dontrun{
Sys.setenv(OPENAI_API_KEY = "your_api_key")
best_obj <- Lense(seurat_obj)
}

}
